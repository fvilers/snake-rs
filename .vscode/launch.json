{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug executable 'snake'",
      "cargo": {
        "args": ["build", "--bin=snake", "--package=snake-rs"],
        "filter": {
          "name": "snake",
          "kind": "bin"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
